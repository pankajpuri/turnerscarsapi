{
  "dependencies": {
    "config": "^3.3.9",
    "express": "^4.18.2",
    "joi": "^13.1.0",
    "mongoose": "^5.0.1",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "test": "jest --env=node --watchAll --verbose "
  },
  "devDependencies": {
    "jest": "^22.2.2",
    "supertest": "^3.0.0"
  }
}
